[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so client-side routes (/services, /about, etc.) work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
