import React from 'react';
import './Hero.css';

const Hero = () => {
  return (
    <section id="hero" className="section hero">
      <img src="Principal1color.png" alt="Logo" className="hero-logo" />
    </section>
  );
};

export default Hero;
